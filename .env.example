# ============================================================================
# Contract Compliance Analyzer - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# Never commit the .env file with real credentials to version control!
# ============================================================================

# ============================================================================
# GROQ API Configuration (Required for RAG Analysis)
# ============================================================================
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# EMAIL NOTIFICATION Configuration (Optional but Recommended)
# ============================================================================
# Email notifications are sent when contract analysis completes

# Your email address (Gmail example shown below)
EMAIL_SENDER=your_email@gmail.com

# For Gmail: Generate App Password at https://myaccount.google.com/apppasswords
# For other providers: Use your email password (if App Password not available)
EMAIL_PASSWORD=xxxx xxxx xxxx xxxx

# SMTP Server configuration (examples provided)
# Gmail:
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587

# Alternative: Outlook/Hotmail
# EMAIL_SMTP_SERVER=smtp-mail.outlook.com
# EMAIL_SMTP_PORT=587

# Alternative: Yahoo Mail
# EMAIL_SMTP_SERVER=smtp.mail.yahoo.com
# EMAIL_SMTP_PORT=587

# Alternative: SendGrid (use 'apikey' as EMAIL_SENDER, your API key as EMAIL_PASSWORD)
# EMAIL_SMTP_SERVER=smtp.sendgrid.net
# EMAIL_SMTP_PORT=587

# Display name for email sender
EMAIL_SENDER_NAME=Contract Analyzer

# ============================================================================
# OPTIONAL: Database Configuration
# ============================================================================
# Currently uses SQLite (default), adjust if needed
DATABASE_PATH=data/analysis_history.db
CACHE_DB_PATH=response_cache.db

# ============================================================================
# OPTIONAL: Logging Configuration
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. GMAIL (Recommended for testing):
#    - Enable 2-Factor Authentication
#    - Go to: https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Windows Computer"
#    - Copy the 16-character password
#    - Paste into EMAIL_PASSWORD (no spaces needed)
#
# 2. OTHER EMAIL PROVIDERS:
#    - Check their SMTP settings
#    - Most use port 587 with STARTTLS
#    - Some may require App Passwords (Yahoo, Outlook)
#
# 3. RESTART APP:
#    - After updating this file, restart Streamlit
#    - The app will detect the new email configuration
#
# ============================================================================
